import { Loader2 } from "lucide-react";

/**Loading Feedback on page transitions / navigation
 *
 *
 */

export default function Loading() {
  return <Loader2 className="mx-auto my-3 animate-spin" />;
}
